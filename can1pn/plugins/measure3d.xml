<krpano>

	<!--
		measure3d.xml
		krpano 1.23

		https://krpano.com/plugins/xmlextensions/#measure3d

		A little helper tool and example for doing measurements in Depthmap-Panos / 3D-Models.
	-->


	<!-- settings -->
	<measure3d
		ui.bool="true"
		ui_pos.normal="left,10,0"
		ui_pos.mobile="lefttop,10,10"
		ui_dragable.bool="true"
		gap.number="0.0"
		showslope.bool="false"
		unit_format="roundval(v,1) + ' cm'"
		/>


	<!-- css styles for the default user-interface -->
	<cssstyles><![CDATA[
		.m3d_panel {
			font-family: "Inter", "Outfit", -apple-system, sans-serif;
			padding: 4px;
			color: #f8fafc;
			min-width: 210px;
		}
		.m3d_title {
			font-size: 13px;
			font-weight: 700;
			text-transform: uppercase;
			letter-spacing: 0.08em;
			color: #94a3b8;
			margin-bottom: 12px;
			padding-left: 8px;
			border-left: 3px solid #F16A24;
			line-height: 1.2;
		}
		.m3d_btn {
			display: flex;
			align-items: center;
			background: rgba(255, 255, 255, 0.05);
			border: 1px solid rgba(255, 255, 255, 0.08);
			border-radius: 8px;
			padding: 10px 14px;
			margin-bottom: 8px;
			cursor: pointer;
			transition: all 0.25s ease;
			text-decoration: none;
			color: #e2e8f0;
			font-size: 13px;
			font-weight: 600;
		}
		.m3d_btn:hover {
			background: #F16A24;
			border-color: #F16A24;
			color: white;
			transform: translateY(-1px);
			box-shadow: 0 4px 12px rgba(241, 106, 36, 0.35);
			text-decoration: none;
		}
		.m3d_btn:active {
			transform: translateY(0);
			box-shadow: none;
		}
		.m3d_icon {
			margin-right: 10px;
			font-size: 15px;
			display: inline-block;
			transition: transform 0.2s ease;
		}
		.m3d_btn:hover .m3d_icon {
			transform: scale(1.2);
		}
		/* Segmented toggle: chuyen giua che do Di va Do */
		.m3d_toggle {
			display: flex;
			background: rgba(255, 255, 255, 0.06);
			border: 1px solid rgba(255, 255, 255, 0.10);
			border-radius: 10px;
			padding: 3px;
			gap: 3px;
			margin-bottom: 10px;
		}
		.m3d_seg {
			flex: 1;
			text-align: center;
			padding: 9px 6px;
			border-radius: 8px;
			font-size: 13px;
			font-weight: 700;
			color: #cbd5e1;
			cursor: pointer;
			text-decoration: none;
			transition: all 0.2s ease;
		}
		.m3d_seg:hover { color: #ffffff; text-decoration: none; }
		.m3d_seg_active {
			background: #F16A24;
			color: #ffffff;
			box-shadow: 0 2px 8px rgba(241, 106, 36, 0.4);
		}
		.m3d_seg_active:hover { color: #ffffff; }
	]]></cssstyles>


	<!-- Render giao dien dong theo che do (walk / measure) -->
	<action name="m3d_render_ui" scope="local" type="Javascript"><![CDATA[
		var isMeasure = (krpano.tourMode === "measure");
		var segWalk   = isMeasure ? "m3d_seg" : "m3d_seg m3d_seg_active";
		var segMeas   = isMeasure ? "m3d_seg m3d_seg_active" : "m3d_seg";

		var html = ""
			+ '<div class="m3d_panel">'
			+   '<div class="m3d_title">Th\u01b0\u1edbc \u0110o 3D</div>'
			+   '<div class="m3d_toggle">'
			+     '<a class="' + segWalk + '" href="event:m3d_mode_walk()">\ud83d\udeb6 \u0110i</a>'
			+     '<a class="' + segMeas + '" href="event:m3d_mode_measure()">\ud83d\udccf \u0110o</a>'
			+   '</div>';

		if (isMeasure) {
			html = html
				+ '<a class="m3d_btn" href="event:start_measuring_between_points(true)">'
				+   '<span class="m3d_icon">\ud83d\udccf</span> \u0110o gi\u1eefa 2 \u0111i\u1ec3m</a>'
				+ '<a class="m3d_btn" href="event:start_measuring_between_surfaces(true)">'
				+   '<span class="m3d_icon">\ud83d\udcd0</span> \u0110o gi\u1eefa 2 b\u1ec1 m\u1eb7t</a>'
				+ '<a class="m3d_btn" href="event:save_measurements()" style="background: rgba(241, 106, 36, 0.12); border-color: rgba(241, 106, 36, 0.20); color: #ff8e4d;">'
				+   '<span class="m3d_icon">\ud83d\udcbe</span> L\u01b0u s\u1ed1 \u0111o</a>';
		}
		html += '</div>';

		if (krpano.get("layer[measure3d_ui]")) {
			krpano.set("layer[measure3d_ui].html", html);
		}
	]]></action>



	<action autorun="onstart" scope="local" type="Javascript">
		var measure3d = krpano.measure3d;

		if(measure3d.ui)
		{
			var ui = krpano.addlayer("measure3d_ui");

			var ui_pos = measure3d.ui_pos.split(",");

			ui.setvars({
				keep:true,
				type:"text",
				align:ui_pos[0], x:ui_pos[1], y:ui_pos[2],
				css:"color:white;",
				bgcolor:0x0f172a,
				bgalpha:0.80,
				bgborder:"1 0xffffff 0.12",
				bgroundedge:14,
				bgshadow:"0 12px 36px rgba(0,0,0,0.4)",
				cursor:"default",
				// Chan click xuyen qua panel xuong 360: layer nuot toan bo su kien chuot.
				capture:true,
				bgcapture:true,
				padding:"12 12"
			});

			// make the layer dragable
			if (measure3d.ui_dragable)
			{
			 	ui.cursor = "move";
				ui.ondown = "changealign(lefttop); asyncloop(pressed, x+=mouse.dx; y+=mouse.dy; clamp(x,0,calc(stagewidth-pixelwidth)); clamp(y,0,calc(stageheight-pixelheight)); );";
			}

			// Danh dau con tro dang o tren panel de handler click cua 360 bo qua,
			// tranh click vao UI van tuong tac voi 360.
			ui.onover = function() { krpano.overMeasureUI = true; };
			ui.onout  = function() { krpano.overMeasureUI = false; };

			// Mac dinh o che do "Di" (walk); render giao dien theo che do.
			krpano.tourMode = "walk";
			krpano.call("m3d_render_ui()");
		}
	</action>



	<!-- styles/designs for the measurement elements -->

	<style name="measure3d_cursor"
		type="text"
		width="9" height="9" scale="0.2"
		oversampling="3"
		mipmapping="true"
		bgcolor="0x000000"
		bgalpha="0.7"
		bgroundedge="25"
		bgborder="20 0xFFFFFF 0.25"
		bgborderblend="false"
		bgshadow="0 0 5 0x000000 0.5"
		distorted="true"
		depth="0"
		depthbuffer="false"
		depthoffset="-100"
		depthwrite="false"
		rotationorder="zxy"
		enabled="false"
		zorder="0"
		/>

	
	<style name="measure3d_marker"
		type="text"
		enabled="false"
		width="9"
		height="9"
		bgroundedge="12"
		oversampling="3"
		mipmapping="true"
		bgcolor="0x000000"
		bgalpha="0.8"
		bgborder="7 0xFFFFFF 0.5"
		bgborderblend="false"
		bgshadow="0 0 2 0x000000 0.8"
		scale="0.25"
		zoom="true"
		distorted="true"
		rotationorder="zxy"
		twosided="true"
		depth="0"
		depthbuffer="false"
		depthwrite="false"
		depthoffset="-200"
		zorder="1"
		/>


	<style name="measure3d_line"
		polyline="true"
		borderwidth="3"
		borderwidth3d="true"
		bordercolor="0x00FF00"
		bordergradient="0xFF000000|0x00FFFFFF|0xFF000000|4"
		borderalpha="0.7"
		depthbuffer="false"
		depthwrite="false"
		depthoffset="-200"
		enabled="false"
		zorder="1"
		/>
		

	<style name="measure3d_linetext"
		type="text"
		css="font-size:14px;"
		textalign="center"
		bgcolor="0xFFFFFF"
		bgalpha="0.8"
		bgborder="1 0x0000000 0.7"
		bgroundedge="7"
		padding="2 4"
		depth="0"
		depthbuffer="false"
		depthoffset="-200"
		zorder="2"
		/>


	<!-- Toggle: chuyen giua che do Di (walk) va Do (measure).
	     O moi che do, che do con lai bi vo hieu hoa. -->
	<action name="m3d_mode_walk" scope="local">
		stop_measuring();
	</action>

	<action name="m3d_mode_measure" scope="local">
		start_measuring_between_points(true);
	</action>

	<!-- API actions for starting/stopping the measurements -->

	<action name="start_measuring_between_points" scope="local" args="showinfo">
		if(showinfo, measure3d_showinfo('double click to start/stop measuring'));
		measure3d_start(1);
	</action>

	<action name="start_measuring_between_surfaces" scope="local" args="showinfo">
		if(showinfo, measure3d_showinfo('double click to measure'));;
		measure3d_start(2);
	</action>
	
	<action name="stop_measuring" scope="local" type="Javascript"><![CDATA[
		krpano.measure3d_loop = false;
		// Bat lai dieu huong cua plugin navigator sau khi ket thuc do.
		krpano.jyNavEnabled = false;
		krpano.get("events").ondoubleclick = null;
		
		krpano.hotspot.forEach(function(h) {
			var style = h.style || "";
			var name = h.name || "";
			var isMeasureHS = (typeof style === "string" && style.indexOf("measure3d") > -1) || 
			                  (typeof name === "string" && name.indexOf("measure3d") > -1);
			if (!isMeasureHS) {
				h.enabled = true;
			}
		});

		// Tro ve che do Di va cap nhat giao dien toggle.
		krpano.tourMode = "walk";
		krpano.call("m3d_render_ui()");
	]]></action>
	
	
	
	<action name="save_measurements" type="js"><![CDATA[
		var xmlcode = '';
		krpano.hotspot.forEach( function(hs)
		{
			if(hs.style == 'measure3d_line' || hs.style == 'measure3d_linetext' || hs.style == 'measure3d_marker')
			{
				xmlcode += hs.getxml() + "\n";
			}
		});
		
		navigator.clipboard.writeText(xmlcode);
		
		krpano.call("measure3d_showinfo('the measurement xml code[br]was copied to the clipboard');");
	]]></action>


	<!-- events: stop on ESC key -->
	<events name="measure3d" keep="true"
		onkeydown="if(keycode == 27, stop_measuring(); );"
		/>


	<action name="measure3d_showinfo" scope="local" args="infotext">
		addlayer('measure3d_infotext',l);
		set(l, type=text, html=get(infotext), css='font-size:20px;font-weight:bold;color:white;text-align:center', bg=false, txtshadow='0 1 4 0x000000 1.0', align=center, enabled=false, alpha=0, width=50%);
		tween(l.alpha, 1.0);
		delayedcall(3.0,
			tween(l.alpha, 0.0, 0.25, default, removelayer(get(l.name)); );
		);
	</action>

	

	<action name="measure3d_start" type="Javascript"><![CDATA[
		
		var measure3d = krpano.measure3d;
		var gap = measure3d.gap;
		
		var mode = 1*args[1];

		// Vô hiệu hóa các hotspot di chuyển khi đo đạc
		krpano.hotspot.forEach(function(h) {
			var style = h.style || "";
			var name = h.name || "";
			var isMeasureHS = (typeof style === "string" && style.indexOf("measure3d") > -1) || 
			                  (typeof name === "string" && name.indexOf("measure3d") > -1);
			if (!isMeasureHS) {
				h.enabled = false;
			}
		});

		// Chan dieu huong cua plugin "navigator" (forward-sensing) khi dang do.
		// navigator_events.onclick chi chuyen scene khi jyNavEnabled == false,
		// nen giu jyNavEnabled = true de khong bi chuyen diem/scene luc click/double click.
		krpano.jyNavEnabled = true;

		// Danh dau dang o che do Do va cap nhat giao dien toggle.
		krpano.tourMode = "measure";
		krpano.call("m3d_render_ui()");

		var hs = krpano.addhotspot("auto", "webgl");
		hs.loadstyle("measure3d_cursor");

		var line = null;
		var line_p1 = null;
		var line_p2 = null;
		var p1marker = null;
		var p2marker = null;

		krpano.events.ondoubleclick = function()
		{
			var hit = null;

			if (line == null)
			{
				line = krpano.addhotspot("auto", "webgl");
				line.loadstyle("measure3d_line");

				line_p1 = line.point.createItem(0);
				line_p2 = line.point.createItem(1);
				line_p1.x = line_p2.x = hs.tx;
				line_p1.y = line_p2.y = hs.ty;
				line_p1.z = line_p2.z = hs.tz;
				
				p1marker = krpano.addhotspot("auto", "webgl");
				p1marker.loadstyle("measure3d_marker");
				p1marker.tx = line_p1.x;
				p1marker.ty = line_p1.y;
				p1marker.tz = line_p1.z;
				p1marker.rx = hs.rx;
				p1marker.ry = hs.ry;
				p1marker.rz = hs.rz;

				if (mode == 2)
				{
					hit = krpano.raycast(hs.tx, hs.ty, hs.tz, hs.nx, hs.ny, hs.nz);
					if (hit)
					{
						line_p2.x = hit.x;
						line_p2.y = hit.y;
						line_p2.z = hit.z;
					}
					else
					{
						krpano.call("measure3d_showinfo('no hit')");
						p1marker.remove();
						line.remove();
						return;
					}
				}
				else
				{
					return;
				}
			}

			// finish
			var dx = line_p2.x - line_p1.x;
			var dy = line_p2.y - line_p1.y;
			var dz = line_p2.z - line_p1.z;
			var length = Math.sqrt(dx*dx + dy*dy + dz*dz);

			var lineinfo = krpano.addhotspot("auto", "webgl");
			lineinfo.loadstyle("measure3d_linetext");

			krpano.v = length;
			krpano.actions.calc("v_formated", measure3d.unit_format);
			lineinfo.html = krpano.v_formated;
			
			if (measure3d.showslope)
			{
				var y_angle = Math.atan2(-dy, Math.sqrt(dx*dx + dz*dz) ) / Math.PI * 180.0;
				
				lineinfo.html += "[br]" + Math.abs(y_angle).toFixed(2) + "°";
			}
			

			lineinfo.deletehtml = "[span style='font-size:18px;color:red;vertical-align:middle;']❌[/span] Delete";

			// place between the points
			lineinfo.tx = 0.5*line_p1.x + 0.5*line_p2.x;
			lineinfo.ty = 0.5*line_p1.y + 0.5*line_p2.y;
			lineinfo.tz = 0.5*line_p1.z + 0.5*line_p2.z;

			lineinfo.onclick = function()
			{
				if (lineinfo.backuphtml)
				{
					lineinfo.hs_line.remove();
					lineinfo.hs_p1marker.remove();
					lineinfo.hs_p2marker.remove();
					lineinfo.remove();
				}
				else
				{
					lineinfo.backuphtml = lineinfo.html;
					lineinfo.html = lineinfo.deletehtml;

					krpano.actions.delayedcall(1, function()
					{
						if (lineinfo.backuphtml)
						{
							lineinfo.html = lineinfo.backuphtml
							lineinfo.backuphtml = null;
						}
					});
				}
			}

			if (krpano.device.mouse)
			{
				lineinfo.onover = function()
				{
					if (!lineinfo.backuphtml)
					{
						lineinfo.backuphtml = lineinfo.html;
						lineinfo.html = lineinfo.deletehtml;
					}
				}

				lineinfo.onout = function()
				{
					if (lineinfo.backuphtml)
					{
						lineinfo.html = lineinfo.backuphtml;
						lineinfo.backuphtml = null;
					}
				}
			}

			p2marker = krpano.addhotspot("auto", "webgl");
			p2marker.loadstyle("measure3d_marker");
			p2marker.tx = line_p2.x;
			p2marker.ty = line_p2.y;
			p2marker.tz = line_p2.z;

			p2marker.rx = (hit || hs).rx;
			p2marker.ry = (hit || hs).ry;
			p2marker.rz = (hit || hs).rz;

			lineinfo.hs_line = line;
			lineinfo.hs_p1marker = p1marker;
			lineinfo.hs_p2marker = p2marker;

			// Giu lai so do khi chuyen scene (cac scene dung chung he toa do 3D).
			line.keep = true;
			p1marker.keep = true;
			p2marker.keep = true;
			lineinfo.keep = true;

			line = null;
			line_p1 = null;
			line_p2 = null;
			p1marker = null;
			p2marker = null;
			
			if (krpano.device.touch)
			{
				// stop after drawing one line on touch devices
				krpano.call("stop_measuring();");
			}
		}

		krpano.measure3d_loop = true;

		krpano.actions.asyncloop("measure3d_loop",
			function()
			{
				var hit = null;

				if (krpano.hoveringelement == null || krpano.hoveringelement == hs || (krpano.hoveringelement && krpano.hoveringelement.hittest))
				{
					hit = krpano.cursorraycast();
				}

				if (hit)
				{
					// optionally add a small gap/offset to the hit surface to avoid depthbuffer rendering problems
					hs.tx = hit.x + hit.nx*gap;
					hs.ty = hit.y + hit.ny*gap;
					hs.tz = hit.z + hit.nz*gap;

					// hotspot rotation
					hs.rx = hit.rx;
					hs.ry = hit.ry;
					hs.rz = hit.rz;

					// store also the normal of the hit-surface for later usage
					hs.nx = hit.nx;
					hs.ny = hit.ny;
					hs.nz = hit.nz;

					hs.visible = true;

					if (line)
					{
						line.visible = true;
					}

					if (line_p2)
					{
						line_p2.x = hs.tx;
						line_p2.y = hs.ty;
						line_p2.z = hs.tz;
					}
				}
				else
				{
					if (line)
					{
						line.visible = false;
					}

					hs.visible = false;
				}
			},
			function()
			{
				if(line) line.remove();
				if(p1marker) p1marker.remove();
				hs.remove();
			}
		);

	]]></action>

</krpano>
